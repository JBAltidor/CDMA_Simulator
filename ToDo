# Todo

- Create exe
- add variable noise level
- Add message when somethings goes wrong exemple :empty message as input )
- try to fix ascii conversion error 

remove 0 as padding replace with random shit (random.randint(0,1) (done)
send a tuple containing the length of each message so we can use binary instead of ternary (done)
enable copy/pasting : https://www.bitforestinfo.com/blog/05/14/how-to-add-copycutpaste-feature-inin-python-python-magicstick.html (done)
Fix error with 2 users + noise (done)
Add error message when ascii conversion fails (done)

#Important 
voltage range is set to [0,1] and [0,-1] and noise level to 0.8 . so lot of errors 